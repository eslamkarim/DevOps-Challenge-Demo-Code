version: "3"
services:
 redis:
    image: "redis:alpine"
    command: ["redis-server", "--appendonly", "yes"]
    expose:
     - 6379
    hostname: redis
    networks:
      - redis-net
    volumes:
     - .:/var/lib/redis
    environment:
     - REDIS_REPLICATION_MODE=master

 app:
    links:
      - redis
    build: ./
    expose:
      - 8000
    volumes:
      - .:/devops-app
    working_dir: /devops-app
    env_file:
      - ./.env
    environment:
      - REDIS_HOST=redis
    networks:
      - redis-net
      
networks:
  redis-net: